# Dependencies
node_modules/
**/node_modules/

# Local agent / MCP deploy scratch (never commit)
agent-tools/
supabase/.deploy/

# Nested product trees (separate repos / not this release line)
# Uncomment if you clone them inside the monorepo by mistake:
# CertiX/
# Miraes/

# Build / deploy artifacts
dist/
sites/docs/.vitepress-cache/
*.zip
Archivo.zip
*.tar.gz
supabase/.temp/
escrow-deployment.json

# Environment / secrets (never commit real values)
.env
.env.*
!.env.example
**/.env
!**/.env.example
.env.local
.env.production
arcusx/.env
local-test/.env
*.pem
id_rsa
id_rsa.pub
**/credentials.json
**/serviceAccount*.json

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# PHP leftover / nested clones (not the production path)
vendor/
**/tsconfig.tsbuildinfo
local-test/tsconfig.tsbuildinfo
