# Optional — also editable in the UI (localStorage)
# Partner sandbox key (SOW3 Week2 agentic + partner suite)
VITE_ARCUSX_API_KEY=axk_test_…
# Leave empty → Vite DEV proxy /partner-api → Edge arcusx-api (Testnet)
VITE_ARCUSX_API_URL=

# Proxy injects these (server-side only; not exposed to browser bundle)
SUPABASE_ANON_KEY=
# Override Edge origin if needed
# ARCUSX_EDGE_ORIGIN=https://atgsesbstjleabesclzs.supabase.co/functions/v1/arcusx-api

# Prefill Testnet wallets (payer on agent.create / prepareFund; worker receiver)
VITE_TEST_CLIENT_WALLET=
VITE_TEST_WORKER_WALLET=
