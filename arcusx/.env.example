
# Legacy PHP (solo si VITE_USE_PHP_API=true)
VITE_API_URL=https://arcusx.pro/api

VITE_SUPABASE_URL=https://example.supabase.co
VITE_SUPABASE_ANON_KEY=anon-key
# Por defecto el front usa Edge Functions si VITE_SUPABASE_URL está definido.
# VITE_USE_PHP_API=true

# Cron Edge (solo Supabase Edge Secrets → ARCUSX_CRON_SECRET; invocar con cPanel cron — ver docs/supabase/CRON_SECRET.md)
# No hace falta en .env del frontend.

# Email notificaciones — solo Supabase Edge Secrets (NO commitear keys)
# Recomendado (HTTP desde Edge; el SMTP de cPanel suele bloquear IPs de Supabase):
# RESEND_API_KEY=re_...
# EMAIL_FROM=ArcusX <notificaciones@arcusx.pro>
# ARCUSX_EMAIL_LOGO_URL=https://arcusx.pro/arcusxmail.jpg
# ARCUSX_EMAIL_PROVIDER=resend
# Fallback SMTP cPanel (solo si el hosting abre puertos externos):
# SMTP_HOST=mail.arcusx.pro
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=notificaciones@arcusx.pro
# SMTP_PASS=
# SMTP_FROM=ArcusX <notificaciones@arcusx.pro>

# Deals — link de pago + escrow (desactivar con false)
# VITE_DEALS_ENABLED=true

# Service role solo para scripts locales (import dump), no en build Vite
ARCUSX_SUPABASE_URL=
ARCUSX_SUPABASE_SERVICE_ROLE_KEY=

# ============================================
# REFERIDOS + PHP / EDGE (NO usar prefijo VITE_)
# NO se incluyen en npm run build. El navegador nunca las ve.
# Copia estos valores al .htaccess del servidor (SetEnv), igual que DB/JWT.
# Ver backend_externo/.htaccess
# ============================================
ARCUSX_SUPABASE_URL=https://example.supabase.co
ARCUSX_SUPABASE_SERVICE_ROLE_KEY=
REFERRAL_INTERNAL_SECRET=
REFERRAL_HASH_SALT=
REFERRAL_ADMIN_MYSQL_IDS=1
# Solo Edge (opcional; ya en Supabase Secrets):
# REFERRAL_CORS_ORIGINS=https://arcusx.pro,https://www.arcusx.pro,http://localhost:5173
# ARCUSX_JWT_SECRET=  # mismo que backend ARCUSX_JWT_SECRET (referral-admin)


VITE_TRUSTLESS_WORK_API_KEY=
VITE_TRUSTLESS_WORK_BASE_URL=development
VITE_PLATFORM_WALLET=
VITE_ADMIN_WALLET=


VITE_STELLAR_NETWORK=testnet


VITE_SOROSWAP_API_BASE=https://api.soroswap.finance
VITE_SOROSWAP_API_KEY=keyyy

VITE_MAIN_SITE_URL=https://arcusx.pro
VITE_ENTERPRISE_APP_URL=https://empresas.arcusx.pro/login
VITE_ENTERPRISE_LANDING_HOST=false
VITE_ENTERPRISE_LANDING_URL=https://empresas.arcusx.pro
