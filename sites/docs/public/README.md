# Optional VitePress static assets (internal preview only)

Production `docs.arcusx.pro` is the React docs frontend in `arcusx/` (hostname `docs.*`).
Do not deploy VitePress output as the public docs site.
